h3 New Bet
= form_for Bet.new, remote: true do |f|
  = render 'events/form', form: f, event_new: Event.new
  = f.label :amount, 'Operation amount'
  = f.text_field :amount
  = render 'operations/form', form: f, operation_new: Operation.new
  = f.submit 'Save'